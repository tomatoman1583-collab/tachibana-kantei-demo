# AGENTS.md — たちばな不動産鑑定事務所（デモサイト）

このリポジトリは、士業サイトのリニューアルを想定した **Web制作ポートフォリオ用のデモサイト** です。
「たちばな不動産鑑定事務所」は架空の事務所で、実在の団体・個人とは一切関係ありません。事務所名・人物名・所在地・電話番号・料金・実績は、すべて架空の設定です。

## 絶対に守ること

- 実在する事務所名・実在の数値（実際の料金相場・実績件数など）・実在サイトの分析結果をこのリポジトリに含めない。参考にした実在サイトのパターンは「型」として抽象化してから使い、文章・数値をそのまま転記しない。
- 各ページのフッターにある架空事務所の注記（「本サイトはWeb制作ポートフォリオ用に作成した架空の事務所です。実在の団体・個人とは関係ありません。」）を削除・弱化しない。
- `compare.html` / `compare-shosai.html` / `kintsugi-ogp.png`（別演習「kintsugi」の前後比較ツール）には触れない。なお`kintsugi-repair.css`という独立ファイルは存在せず、実体は`compare.html`/`compare-shosai.html`内のインライン`<style id="kintsugi-repair">`要素。

## 想定ペルソナ

**主ペルソナ**：相続・離婚など、人生のライフイベントで不動産の適正価格を知りたい個人。「依頼する場面を想像しにくい」という弱点を正面から解消することを最優先にする。

**ターゲット3層**：
1. **個人のお客様**（ご依頼の中心）— 相続で不動産を分けたい／離婚で財産分与したい／売る前に適正価格を知りたい
2. **法人のお客様** — 同族会社間の不動産売買、社宅・店舗の適正賃料、決算・組織再編にともなう資産評価
3. **士業・専門家の方** — 弁護士・税理士・司法書士からの、調停・訴訟提出用や相続税申告添付用の鑑定評価依頼

## 判断が割れたときのルール

- 「不動産会社の査定」と「不動産鑑定士の鑑定評価」の違いを曖昧にしない。査定は無料・目安・公的効力なし、鑑定評価は有料・国の基準に基づく・裁判所や税務署に提出できる、という区別を毎回明示する。
- 相続税申告に関する記述は、「鑑定評価は原則ではなく、路線価等が実態に合わない場合の裏付け資料」という留保を必ず添える（過去のverifierで指摘・修正済み）。
- 料金は非公開にせず、目安のレンジで必ず提示する（参考にした実在サイトの弱点＝料金非開示を踏襲しない）。

## 制作の方針

- **デザイン方向性**：モダン・ミニマル型。白基調＋電光ブルー（`--gold:#1E40FF`）のアクセント、直線的・低装飾（角丸2px、影なし）。
- **写真は絞って使う**：実写真は **4点だけ**。トップページのヒーロー1点と、`cases.html`（解決事例）・`about.html`（代表紹介・事務所概要）・`contact.html`（お問い合わせ）の3ページに置くヘッダー帯1点ずつ。**残りの3ページ（`services.html`・`flow.html`・`faq.html`）には写真を置かない**。アイコン・ロゴ・シルエットは引き続きすべてインラインSVGで構成する。写真の出典は `CREDITS.md` を参照。
  - **なぜ全ページに入れないか**：全ページに写真を敷くと、このサイトの持ち味である「白と余白の静かさ」が薄まり、モダン・ミニマルの方向性が崩れる。そこで**感情が動くページ（解決事例＝自分の状況を重ねる／お問い合わせ＝相談する場面を思い浮かべる）と、信頼をつくるページ（代表紹介・事務所概要＝どんな人がどんな場所でやっているか）**の3つに絞る。料金・手順・FAQは情報を確かめに来るページなので、写真は判断の助けにならず、読み込みが増えるだけになる。
  - **ヘッダー帯の作り方（3ページで揃える）**：`<main>` の `<div class="section-head">` の直前に `<figure class="page-banner">` を1つ置く。3ページとも同じクラス・同じ 16:5・同じ位置にする。画像は**表示比率と同じ 16:5 に切り出してから**派生を作り、`<picture>` ＋ `<source type="image/webp">` ＋ JPEGフォールバックの `<img>`（`width`/`height` 属性つき・`loading="eager"`・`decoding="async"`）で配信する。CSSは `width:100%;height:auto` と `border-radius:var(--radius-lg)` だけで、`aspect-ratio` や `object-fit` は使わない。`alt` は画像の内容を日本語で書き、空altにしない（装飾ではなく内容の一部として扱う）。
- **レスポンシブ・アクセシビリティ重視**：コントラスト比はAA基準（4.5:1）以上を確保する。横スクロールが発生しないよう、グリッド・表は `overflow-x:auto` のラッパーに入れる。
- **構成**：7ページ（トップ／支援内容・料金／解決事例／ご相談の流れ／よくある質問／代表紹介・事務所概要／お問い合わせ）。各ページ共通のヘッダー・フッターを静的に複製し、ナビゲーションは実ページ遷移（`<a href="...">`）で構成する。

## 制作の経緯

単一HTMLファイル（Claude Artifact用・JSでページ切り替えするSPA形式）として3ラウンドの独立検証（verifier）を経て内容を確定させたのち、実ファイル複数ページ＋GitHub Pages公開の形式へ再構成した。可視テキスト（コピー・料金・FAQ・免責文言・数値）は再構成の前後で一切変更していない。
